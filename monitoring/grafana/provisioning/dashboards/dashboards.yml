apiVersion: 1

providers:
  - name: QuizArena
    orgId: 1
    folder: QuizArena
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
